{
  "name": "@workspace/agent-core",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "MatchRV agent-native inventory layer: canonical RV schema with provenance, deterministic matching/explanation engine, and the WebMCP tool contracts. Pure TypeScript — runs on the server and in the browser.",
  "exports": {
    ".": "./src/index.ts",
    "./snapshot": "./data/inventory.snapshot.json"
  },
  "scripts": {
    "build-snapshot": "tsx scripts/build-snapshot.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
